[project]
name = "aws-appconfig-demo"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["boto3>=1.34.0", "fastapi>=0.111.0", "uvicorn[standard]>=0.30.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
